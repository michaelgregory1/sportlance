<%= simple_form_for(@booking) do |f| %>
  <%= f.input :user_id, as: :hidden, input_html: { value: @booking.user_id } %>
  <%= f.input :location_id %>
  <%= f.input :date_start %>
  <%= f.input :date_end %>
  <%= f.submit class: "btn" %>
<% end %>
